name: Keep Render alive

on:
  schedule:
    - cron: '*/14 * * * *'   # every 14 min — Render sleeps after 15 min of inactivity

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -f https://larder.onrender.com/health
